# Core (always needed)
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0

# OpenVINO (for InferenceEngine, SketchClassifier, optional VLM)
openvino>=2024.4.0
openvino-genai>=2024.4.0
huggingface_hub>=0.23.0

# Tokenizer auto-conversion (only needed when using --vlm with community-converted repos)
transformers>=4.40.0
sentencepiece>=0.2.0
tiktoken>=0.7.0

# Optional: voice input (Whisper STT)
# sounddevice>=0.4.6

# Tests
pytest>=7.0
